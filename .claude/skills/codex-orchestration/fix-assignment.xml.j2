---
name: fix-task
version: 1.3.0
description: lead post-triage fix assignment template for codex-orchestration QA findings.
format: xml
required_variables:
  - task_id
  - phase
  - sprint_doc
  - branch
  - worktree_path
  - pr_target
  - description
  - finding_ids
  - triage_records
  - required_fixes
  - acceptance_criteria
  - references
optional_variables:
  - sprint_id
  - lead
  - cc
  - validation_requirements
  - dependency_relations
defaults:
  sprint_id: ""
  lead: team-lead
  cc: team-lead
  validation_requirements: ""
  dependency_relations: "- See authoritative sprint doc."
metadata:
  type: fix-task
  tags:
    - stage:fix
  workflow:
    scope:
      kind: phase
      variable: phase
    state: fix-start
    stage: fix
    transition: start
---
<atm-task id="{{ task_id }}" phase="{{ phase }}" mode="fix">
  <description><![CDATA[{{ description | string | cdata_escape }}]]></description>

  <branch><![CDATA[{{ branch | string | cdata_escape }}]]></branch>
  <worktree><![CDATA[{{ worktree_path | string | cdata_escape }}]]></worktree>
  <pr-target><![CDATA[{{ pr_target | string | cdata_escape }}]]></pr-target>
  <stack-discipline><![CDATA[`{{ branch | string | cdata_escape }}` is a layer cut from the current top of the phase stack; `{{ pr_target | string | cdata_escape }}` is the layer directly below it. Work only on this layer. Never edit, push to, rebase, or open a PR against any layer below, and never wait for a lower layer's QA or CI. If `{{ pr_target | string | cdata_escape }}` gains commits while you work, run `git fetch origin && git merge origin/{{ pr_target | string | cdata_escape }}` once at task start and once before the final push; the parent wins where copies differ. Once this task closes the layer is frozen: every later finding against it is fixed on a new layer cut from the top of the stack, never here.]]></stack-discipline>
  <authoritative-sprint-doc><![CDATA[{{ sprint_doc | string | cdata_escape }}]]></authoritative-sprint-doc>

  <dependency-relations><![CDATA[
{{ dependency_relations | string | cdata_escape }}
  ]]></dependency-relations>

  <finding-ids><![CDATA[
{{ finding_ids | string | cdata_escape }}
  ]]></finding-ids>

  <triage-records><![CDATA[
{{ triage_records | string | cdata_escape }}
  ]]></triage-records>

  <required-fixes><![CDATA[
{{ required_fixes | string | cdata_escape }}
  ]]></required-fixes>

  <acceptance-criteria><![CDATA[
{{ acceptance_criteria | string | cdata_escape }}
  ]]></acceptance-criteria>

  <validation-requirements><![CDATA[
{{ validation_requirements | string | cdata_escape }}
  ]]></validation-requirements>

  <references><![CDATA[
{{ references | string | cdata_escape }}
  ]]></references>

  <workflow>
    <step id="a"><![CDATA[Start the task immediately with `atm task start {{ task_id | string | cdata_escape }} "<one line: what you will do first>"`, then claim the matching bead with `bd update {{ task_id | string | cdata_escape }} --claim` and begin executing in the same work session unless blocked.]]></step>
    <step id="b"><![CDATA[Read `{{ sprint_doc | string | cdata_escape }}` before editing. Treat it as the authoritative source for sprint scope, retained requirements, deletion targets, validation, and acceptance criteria. If this assignment summary and the sprint doc differ, the sprint doc wins and the mismatch must be reported.]]></step>
    <step id="b1"><![CDATA[If this task touches Rust, read `.claude/skills/rust-development/guidelines.txt` and `.claude/skills/rust-best-practices/patterns/practice-inventory.md` before editing; the reviewers score against them.]]></step>
    <step id="c"><![CDATA[Update the assigned worktree from the target branch before editing. Include all earlier-sprint fixes already merged forward to that branch.]]></step>
    <step id="c1"><![CDATA[Honor the `<dependency-relations>` block above: must-follow merge-forward trigger is parent dev push, not QA; merge parent → child before every round. PR-completion trigger is parent PR merge first. Stop if a parallel-safe relation intersects.]]></step>
    <step id="d"><![CDATA[Read the referenced triage records first. Treat the listed occurrences as the minimum required scope on this branch.]]></step>
    <step id="d2"><![CDATA[Before fixing each finding, verify the defect it describes is still actually present at the cited file:line in this worktree's current state. If a finding does not reproduce — already fixed, superseded by other work, or the described code path no longer exists — refuse to perform speculative or invented work to "address" it. Do not edit unrelated code in its name. Note it in your push/validation report as `not-reproducible` with the file:line you checked instead, and skip it. This applies per-finding, not to the whole task — fix every finding that does reproduce as normal.]]></step>
    <step id="e"><![CDATA[Fix every promoted-branch occurrence in scope. Do not stop at the first location when the triage record marks the finding as repeatable.]]></step>
    <step id="f"><![CDATA[As soon as the branch is pushable, commit and push immediately. Send branch name and commit hash right away to the assigner (the sender of this message) with a plain `atm send {{ lead | string | cdata_escape }} --stdin` — a progress report, not a close; never close the task on a push. This lets follow-up QA start in parallel.{% if cc and cc != lead %} Copy a one-line summary to `{{ cc | string | cdata_escape }}` with a plain `atm send {{ cc | string | cdata_escape }} --stdin`.{% endif %}]]></step>
    <step id="g"><![CDATA[Run the required validation after the push report. While it is FAIL, keep fixing and report progress with plain `atm send`. When it is PASS, push, and close the task with the validation report rendered from the fix-complete template: `atm task close {{ task_id | string | cdata_escape }} completed --template .claude/skills/codex-orchestration/fix-complete.md.j2 --vars <scratch>/fix-complete-{{ task_id | string | cdata_escape }}-vars.json`, followed by `bd close {{ task_id | string | cdata_escape }}`. Build the vars from that template's `required_variables`: `task_id` and `phase` unchanged from this assignment, `commit` the final pushed head, the gate output lines, and per finding fixed or `not-reproducible` with the file:line checked. Write the vars file outside the repository and never commit it. Only this close frees your queue: the next assignment stays queued and never starts until `{{ task_id | string | cdata_escape }}` is closed, and a plain `atm send` report does not close it. Use `atm task close {{ task_id | string | cdata_escape }} refused "<reason>"` only when the whole assignment cannot be done; leave its bead open and add `bd update {{ task_id | string | cdata_escape }} --notes "<reason>"`.{% if cc and cc != lead %} Copy a one-line summary to `{{ cc | string | cdata_escape }}` with a plain `atm send {{ cc | string | cdata_escape }} --stdin`.{% endif %}]]></step>
{% if triage_records and triage_records|trim %}    <step id="g1"><![CDATA[Triage records are attached to this assignment. Use the `/closing-triage` skill to work them to zero before reporting: `{{ branch | string | cdata_escape }}` is a stacked layer no phase structure declares, so its query runs as `python3 .claude/skills/closing-triage/scripts/query_open_findings.py --branch {{ branch | string | cdata_escape }}{% if sprint_id %} --sprint {{ sprint_id | string | cdata_escape }}{% endif %} --phase {{ phase | string | cdata_escape }} --json` from your worktree. The finding ids it returns are the full scope of this assignment; fix every one in this pass.]]></step>
{% endif %}    <step id="h"><![CDATA[After the close, read ATM again: the next queued task starts only now.]]></step>
  </workflow>
</atm-task>
